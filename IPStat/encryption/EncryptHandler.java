import java.io.File;

public class EncryptHandler {
	/**
	 * Creates EncryptHandler, takes in data to encrypt, secret key and name for
	 * encrypted file.
	 */
	public EncryptHandler(File data, File secKey, String encData) {

	}
}
