import java.io.File;

public class DecryptHandler {
	/**
	 * Creates DecryptHandler, takes in encrypted data, secret key and a string
	 * to name decrypted data file.
	 */
	public DecryptHandler(File encData, File secKey, String decData) {

	}
}
